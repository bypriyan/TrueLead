<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CEO" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/appColor</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/appColor</item>
        <item name="tint">@color/white</item>
        <item name="postSplashScreenTheme">@style/Theme.CEO</item>
    </style>

    <style name="Theme.CEO" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/appColor</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>